;;I like having frequenly used commands being shortened 
;;Even though using smex allows me to cylce through them quickly. Having them like this is also nice. 



(defalias 'cmd 'shell-command); a NEW THING
(defalias 'kcb 'kill-buffer);;is supposed to stand for kill current buffer but for now it's just kill buffer. 
(defalias 'lsp 'list-packages)
(defalias 'sr 'sr-speedbar-toggle)

